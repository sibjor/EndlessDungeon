#define PROJECT_ROOT_PATH "EndlessDungeon"

void FindPath(char* path){
    // Navigate up the directory tree, stop at project root

    // Search all directories in project root for path
}

// Find or create Project root path
void GetProjectRootPath(){
}

// Find or create Metadata path
void GetMetadataPath

// Find or create "Raw Assets" path

// Find or create Assets to use path

// Find or create Spritesheet path